# ═══════════════════════════════════════════════════════════
#  NeViS Backend v3  —  Environment Configuration
#  Copy this file to .env and fill in your values.
#  NEVER commit your .env file to version control.
# ═══════════════════════════════════════════════════════════

# ── Server ─────────────────────────────────────────────────
PORT=5000
NODE_ENV=development
# Options: development | production | test

# ── MongoDB ────────────────────────────────────────────────
# Local MongoDB:
MONGO_URI=mongodb://127.0.0.1:27017/nevis
# MongoDB Atlas (comment out the local one above and use this):
# MONGO_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/nevis?retryWrites=true&w=majority

# ── JWT ────────────────────────────────────────────────────
# Generate secrets with:
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=REPLACE_WITH_64_CHAR_RANDOM_HEX
JWT_REFRESH_SECRET=REPLACE_WITH_ANOTHER_64_CHAR_RANDOM_HEX

# Access token lifetime  (e.g. 15m, 1h, 2h)
JWT_ACCESS_EXPIRES=15m
# Refresh token lifetime (e.g. 7d, 14d, 30d)
JWT_REFRESH_EXPIRES=7d

# ── Email (Nodemailer) ─────────────────────────────────────
# For Gmail: enable "Less secure app access" OR use an App Password (recommended)
# Gmail App Password guide: https://support.google.com/accounts/answer/185833
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your@gmail.com
EMAIL_PASS=your_gmail_app_password
EMAIL_FROM_NAME=NeViS
EMAIL_FROM_ADDR=your@gmail.com

# ── Site / CORS ────────────────────────────────────────────
# The URL your frontend runs on — used in reset-password emails
SITE_URL=http://localhost:5500

# Comma-separated list of allowed CORS origins
ALLOWED_ORIGINS=http://localhost:5500,http://127.0.0.1:5500

# ── Security ───────────────────────────────────────────────
# Bcrypt cost factor (10-14 recommended; higher = slower but safer)
BCRYPT_ROUNDS=12

# Password-reset token expiry in ms (900000 = 15 minutes)
RESET_TOKEN_EXPIRY_MS=900000

# ── Logging ────────────────────────────────────────────────
# Log level: error | warn | info | http | debug
LOG_LEVEL=info
# Directory for log files (relative to project root)
LOG_DIR=logs
